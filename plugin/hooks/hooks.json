{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/session_start.sh\" || true"
          },
          {
            "type": "command",
            "command": "python3 -c \"import json,os,shlex,sys; d=json.load(sys.stdin); f=os.environ.get('CLAUDE_ENV_FILE'); open(f,'a',newline='\\n').write('export CLAUDE_SESSION_ID=%s\\nexport CLAUDE_TRANSCRIPT_PATH=%s\\n'%(shlex.quote(d.get('session_id','')),shlex.quote(d.get('transcript_path','')))) if f else None\" 2>/dev/null; true"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "CAO_NO_AUTOSTART=1 python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/cao-companion.py\" session.shutdown \"\" 2>/dev/null; true"
          }
        ]
      }
    ]
  }
}